server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka-server
          uri: https://github.com/Ricardo940/microservicios-spring
          search-paths: config-data
  application:
    name: config-server